import { memo} from 'react'
import HomeWrapper from './styled'

const Home = memo(() => {

  return (
    <HomeWrapper className="home-container">
     <h1>Foss React Template</h1>

    </HomeWrapper>
  )
})

export default Home
